version: '3.1'

services:
  server:
    image: suttyread/react-server
    ports:
      - '9000:9000'
    expose:
      - '9000'
  client:
    image: suttyread/react-client
    ports:
      - '3000:3000'
    expose:
      - '3000'